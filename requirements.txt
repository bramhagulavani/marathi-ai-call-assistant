# LLM: Groq API (free tier, needs GROQ_API_KEY), used in streaming mode
groq

# Text-to-Speech: edge-tts (free, no API key, streams raw audio bytes —
# replaces gTTS, which only supported one-shot file downloads)
edge-tts

# Web server: FastAPI + WebSocket support for the persistent, full-duplex
# browser connection. [standard] pulls in the websockets library uvicorn
# needs to actually serve WebSocket connections.
fastapi
uvicorn[standard]

# Loads GROQ_API_KEY etc. from a .env file
python-dotenv
